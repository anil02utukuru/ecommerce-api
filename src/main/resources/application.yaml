spring:
  application:
    name: demo
  jackson:
    time-zone: UTC
    serialization:
      write-dates-as-timestamps: false

server:
  port: ${PORT:8080}

logging:
  level:
    root: INFO
    com:
      ecommerce: DEBUG


